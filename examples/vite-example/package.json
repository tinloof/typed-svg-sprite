{
  "name": "vite-example",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "pnpm -C ../.. run build && vite build && tsc --noEmit",
    "dev": "vite",
    "preview": "vite preview"
  },
  "dependencies": {
    "@tinloof/typed-svg-sprite": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "vite": "^8.1.5"
  }
}
